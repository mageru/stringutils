package main

import (
	"strings"
)

func Upper(str string) {
	var upper_str = strings.ToUpper(string)
	return upper_str
}

func Lower(str string) {
	var lower_str = strings.ToLower(string)
	return lower_str
}
